//: [Previous](@previous)

import Foundation

struct Bootcamp {
    var id: String
    var name: String?
    
    // El struct crea un inicializador por defecto, algo que no hacen las clases
}

let boot = Bootcamp(id: "Esto es un id")
